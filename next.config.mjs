/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/test-next-app',
  assetPrefix: '/test-next-app',
  output: 'standalone'
};

export default nextConfig;
